/*
*  Array.prototype.findIndex() 返回通过测试的 当前元素在数组中的下标
*  语法：
*    arr.findIndex(callback[, thisArg])
*  参数
*   + callback(item, index, arr)
*   + thisObj
*  返回值
*   +  数组中通过提供测试函数的第一个元素的索引。否则，返回-1
* */
