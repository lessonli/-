/* 判断一个字符串是否包含在另一个字符串中, 根据情况返回 true false
* 语法
*   str.includes(searchString[, position])
* 参数
*   + searchString 在此字符串中搜索的字符串
*   + position 可选 默认值为 0
*  */

const string = 'lesson'
console.log(string.includes('l'))
